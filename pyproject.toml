[project]
name = "purchase-toolkit-ops"
version = "0.1.0"
description = "Automatizaciones operativas que usan purchase-toolkit."
requires-python = ">=3.14"
dependencies = [
    "purchase-toolkit",
]

[tool.uv]
package = false

[tool.uv.sources]
purchase-toolkit = { git = "https://github.com/ochoaenzo/purchase-toolkit.git" }
